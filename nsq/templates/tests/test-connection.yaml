apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsq.fullname" . }}-test-connection"
  labels:
{{ include "nsq.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nsq.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
